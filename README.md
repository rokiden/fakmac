# FAKeMAC
is minimal Linux kernel module that creates a virtual network interface that looks like a physical one. 

See Makefile for basic usage.

Parameters:
- numdummies: Number of dummy network interfaces to create (default: 1)